image: Visual Studio 2022

configuration: Release

platform: ARM

build_script:
  - msbuild RodnikiRadio.sln ^
      /p:Configuration=Release ^
      /p:Platform=ARM ^
      /p:AppxBundle=Never ^
      /p:UapAppxPackageBuildMode=StoreUpload

artifacts:
  - path: '**\AppPackages\**\*.appx'
    name: RodnikiRadio_ARM